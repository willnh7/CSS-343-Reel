
/**
 * non canonical compression:
 * 
 * 
 * canonical compression:
 * 
 * 
 * 
 * 
 * 
 * A table of symbols and their corresponding Huffman codes, ordered by code length ASC, symbol ASC :
 * 
 * 
 * The first 8 bytes of the Huffman coded passage, printed as binary:
 * A simple loop for this?, first 8 bytes so essentially 
 * for(int i = 0; i < coded passage.size(); i++) {
 * do shet.
 * if(i == 8){break;}
 * 
 * }
 * 
 * 
 * The size in bits and bytes of the compressed passage:
 * bytes vector and a total bits written? to show the size of the compression
 * 
 * The compression ratio.  That is, the size in bits of the compressed passage divided by the size in bits of the uncompressed passage:
 * Keep track of the uncompressed? then keep track of the 
 * 
 */







