#include <iostream>
#include <string>

using namespace std;

class SalesLead {
    public:
        string name;
        int income;
};

class CustomComparator {
    // Priority over income, then whoever has been in the queue first.
    // so if this->income < that->income, return that?
    // the 
};
/**
 * This will test the functionality of the priority queue.
 */
void testMyMoney() {

}